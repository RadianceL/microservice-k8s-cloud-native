spring:
  shardingsphere:
    rules:
      sharding:
        default-database-strategy:
          standard:
            sharding-column: timestamp
            sharding-algorithm-name: standardDatabaseShardingAlgorithm
    datasource:
      db1:
        name: db1
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://124.222.147.194:3306/nacos_devtest?useUnicode=true&characterEncoding=utf8&tinyInt1isBit=false&useSSL=false&allowMultiQueries=true&serverTimezone=Asia/Shanghai
        username: nacos
        password: nacos
        validationQuery: SELECT 1 FROM DUAL
      names: db1